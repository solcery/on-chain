pub mod container;
pub mod game;
/// Game runtime state is tored here
pub mod game_state;
pub mod player;

/// Container with a trait for tighting [AccointInfo](solana_program::account_info::AccountInfo) to
/// datatypes stored inside
pub mod bundled;
